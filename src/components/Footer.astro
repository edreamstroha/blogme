---
const url = Astro.request.url
const page = url.split("/")[4]
let message = ""
if(page == ""){
message = "read the fun kanji above"
} else if (page == "me"){
message = "yoo thats mee!!"
} else if (page == "posts"){
message = "posts posts and posts"
}

---
<div class="footer">
    <div class="line"></div>
    <div class="footer-content">
        <p>welcome to the blog.</p>
        <p>{message}</p>
    </div>
</div>

<style>
    .footer {
        width: 100%;
        height: 100%;
        padding: 5px 10px 5px 10px;
        display: flex;
        gap: 5px;
        flex-direction: column;
        justify-content: flex-start;
    }

    .line {
        width: 50%;
        height: 2px;
        background-color: #6e6a86;
        align-self: flex-end;
    }

    .footer-content{
        flex: 1;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }
</style>
